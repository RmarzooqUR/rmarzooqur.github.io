export const data = {
    layout: 'post',
    title:  "Configuring nginx to work as CDN",
    date:   '2025-04-22 +0530',
    categories: ['internet', 'load balancer'],
    summary: 'Configuring nginx to work as a CDN, a proxy with edge caching',
}

<div className="flex justify-center mb-8">
<h1>{data.title}</h1>
</div>

## What we will learn
- Build nginx with external packages
- 